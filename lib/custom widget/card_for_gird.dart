import 'package:flutter/material.dart';

class CardForGird extends StatelessWidget {
  const CardForGird({super.key});

  @override
  Widget build(BuildContext context) {
    return Ink(child: Card(color: Colors.white));
  }
}
